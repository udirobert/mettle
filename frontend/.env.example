AGENT_URL=http://localhost:8123
OPENAI_API_KEY=

# Rehearsal voice (Person A) — Deepgram Voice Agent for the Opponent phase.
DEEPGRAM_API_KEY=

# Alternative LLM provider for the backend phase nodes (OpenAI-compatible).
# Set in the repo-root .env too — that's what the LangGraph server reads.
OPENROUTER_API_KEY=
# OPENROUTER_MODEL=openai/gpt-4o-mini

# --- CopilotKit Intelligence (optional; set COPILOTKIT_LICENSE_TOKEN to enable Threads — server + UI) ---
# COPILOTKIT_LICENSE_TOKEN=
# INTELLIGENCE_API_URL=http://localhost:4201
# INTELLIGENCE_GATEWAY_WS_URL=ws://localhost:4401
# INTELLIGENCE_API_KEY=
